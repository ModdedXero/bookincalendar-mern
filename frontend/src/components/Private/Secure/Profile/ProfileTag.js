import React from "react";

export default function ProfileTag({ tag }) {
    const getTagType = () => {
        switch (tag) {
            // Add tag returns
        }
    }

    return (
        <div>
            Tag
        </div>
    )
}
